/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./**/*.html'],
    theme: {
      extend: {
        // We can extend the default theme here if needed
      },
    },
    plugins: [],
  }